export interface ISystemConfig {
  host: string;
  port: number;
}

export interface ITargetProducerConfig {
  targetHost: string;
  targetPort: number;
}

export interface ICertConfig {
  certPath: string;
}
